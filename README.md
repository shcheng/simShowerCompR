# simShowerCompR

This R script analyzes the classification of simulated CR showers by first applying PCA and then a ML classifier (Naive Bayes, CART and SVM).

This is intended to check whether the classification can be done with simplest possible methods.
